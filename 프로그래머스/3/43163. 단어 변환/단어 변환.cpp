#include <string>
#include <vector>

using namespace std;

// 전체: DFS 사용
// 문제: 특정 문자열 간 얼마나 차이나는지를 어떻게 하면 효율적으로 판단할까?
int solution(string begin, string target, vector<string> words) {
    int answer = 0;
    return answer;
}